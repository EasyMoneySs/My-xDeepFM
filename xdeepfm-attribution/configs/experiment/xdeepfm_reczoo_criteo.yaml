experiment:
  name: "xdeepfm_reczoo_criteo"

  # 数据/模型/训练配置
  data_config: "configs/data/reczoo_criteo.yaml"
  model_config: "configs/model/xdeepfm.yaml"
  train_config: "configs/train/default.yaml"

  # 输出目录（日志、指标、checkpoint 等）
  output_dir: "./runs/xdeepfm_reczoo_criteo"
  use_timestamp: true  # 是否在输出目录后追加时间戳，便于区分多次运行

  # 随机种子
  seed: 42
